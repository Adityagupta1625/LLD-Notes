package ParkingLot.ParkingManager;

import ParkingLot.ParkingStrategy.ParkingStrategy;

public class TwoWheelerParkingManager extends ParkingManager {

    public TwoWheelerParkingManager(ParkingStrategy parkingStrategy) {
        super(parkingStrategy);
    }
}
